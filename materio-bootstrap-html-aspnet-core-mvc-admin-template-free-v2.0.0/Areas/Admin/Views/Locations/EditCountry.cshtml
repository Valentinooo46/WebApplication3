@model CountryInputModel
@{
    ViewData["Title"] = "Редагування країни";
}

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header d-flex align-items-center justify-content-between">
        <div>
          <h5 class="mb-1">Редагування країни</h5>
          <small class="text-muted">Оновіть назву та збережіть зміни</small>
        </div>
        <a asp-action="Index" class="btn btn-text-secondary">
          <i class="ri-arrow-go-back-line me-1"></i>Повернутись
        </a>
      </div>
      <div class="card-body">
        <form asp-action="EditCountry" method="post">
          @Html.AntiForgeryToken()
          <input asp-for="Id" type="hidden" />
          <div class="form-floating form-floating-outline mb-4">
            <input asp-for="Name" class="form-control" placeholder="Назва країни" />
            <label asp-for="Name"></label>
            <span asp-validation-for="Name" class="text-danger small"></span>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <a asp-action="Index" class="btn btn-outline-secondary">Скасувати</a>
            <button type="submit" class="btn btn-primary">
              <i class="ri-save-3-line me-1"></i>Зберегти
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

@section PageScripts {
  <partial name="_ValidationScriptsPartial" />
}
