@* @model IEnumerable<WebApplication3.Models.Country>

@{
    ViewData["Title"] = "Список держав";
}

<h1>Список стран</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Currency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImagePath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Currency)
                </td>
                <td>
                    @{
                        // Используем ImagePath, который мы сохранили локально в wwwroot/images/
                        if (!string.IsNullOrEmpty(item.ImagePath))
                        {
                            <img src="@Url.Content(item.ImagePath)" alt="Flag" style="width:100px;height:auto;" />
                        }
                        else
                        {
                            <span>No Image</span>
                        }
                    }
                </td>
                <td>
                    @Html.ActionLink("Подробнее", "Details", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table> *@
